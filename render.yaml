services:
  - type: web
    name: portfolio
    env: python
    buildCommand: bash build.sh
    startCommand: cd backend && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.4
